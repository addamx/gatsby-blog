import React from 'react'
import { Link } from "gatsby"

import style from './header.module.scss'


export default props => (
    <div className={1}>
        <div className={style.headband}></div>
        {/* <h1>{props.headerText}</h1> */}
    </div>
)